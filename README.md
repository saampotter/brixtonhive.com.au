This project uses [Next.js](https://nextjs.org/). To run the project locally:

1.  Install JS dependencies

    ```sh
    npm install
    ```

2.  Start the dev server
    ```sh
    npm run dev
    ```
